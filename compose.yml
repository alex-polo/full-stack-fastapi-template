name: full-stack-fastapi-template

x-logging: &default-logging
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

services:
  db:
    image: postgres:18.1
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    environment:
      POSTGRES_USER: ${POSTGRES_USER?Variable not set}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD?Variable not set}
      POSTGRES_DB: ${POSTGRES_DB?Variable not set}
      PGDATA: /var/lib/postgresql/data
    volumes:
      - db-data:/var/lib/postgresql
    networks:
      - app-network
    restart: unless-stopped
    <<: *default-logging

  pgadmin:
    image: dpage/pgadmin4:9.10
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${PGADMIN_PORT?Variable not set}:80"
    environment:
      PGADMIN_LISTEN_PORT: 80
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL?Variable not set}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD?Variable not set}
    volumes:
      - pgadmin-data:/var/lib/pgadmin
    networks:
      - app-network
    restart: unless-stopped
    <<: *default-logging

  backend:
    image: "${DOCKER_IMAGE_BACKEND?Variable not set}:${TAG-latest}"
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
      pgadmin:
        condition: service_started
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -f http://localhost:${BACKEND_PORT?Variable not set}/api/v1/utils/health-check",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    extra_hosts:
      - "glitchtip.local:host-gateway"
    environment:
      BACKEND__ENVIRONMENT: ${ENVIRONMENT?Variable not set}
      BACKEND__DOMAIN: ${DOMAIN?Variable not set}
      BACKEND__PROJECT__PROJECT_NAME: ${PROJECT_NAME?Variable not set}
      BACKEND__PROJECT__DESCRIPTION: ${PROJECT_DESCRIPTION?Variable not set}
      BACKEND__API_PREFIX_PREFIX: ${BACKEND_API_PREFIX?Variable not set}
      BACKEND__GUNICORN__PORT: ${BACKEND_PORT?Variable not set}
      BACKEND__DATABASE__HOST: ${POSTGRES_SERVER?Variable not set}
      BACKEND__DATABASE__PORT: ${POSTGRES_PORT?Variable not set}
      BACKEND__DATABASE__USER: ${POSTGRES_USER?Variable not set}
      BACKEND__DATABASE__USER_PASSWORD: ${POSTGRES_PASSWORD?Variable not set}
      BACKEND__DATABASE__DB_NAME: ${POSTGRES_DB?Variable not set}
      BACKEND__LOGGING__SENTRY_DSN: ${SENTRY_DSN?Variable not set}
      BACKEND__LOGGING__SENTRY_LOG_LEVEL: ${SENTRY_LOG_LEVEL?Variable not set}
      BACKEND__ROOT_USER_EMAIL: ${ROOT_USER_EMAIL?Variable not set}
      BACKEND__ROOT_USER_PASSWORD: ${ROOT_USER_PASSWORD?Variable not set}
      BACKEND__ROOT_USER_IS_CREATE: ${ROOT_USER_IS_CREATE?Variable not set}
    volumes:
      - certificates-data:/app/certificates
    networks:
      - app-network
    restart: unless-stopped
    <<: *default-logging

  client:
    image: "${DOCKER_IMAGE_FRONTEND?Variable not set}:${TAG:-latest}"
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${BACKEND_API_PREFIX?Variable not set}
        - NODE_ENV=production
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "${CLIENT_PORT?Variable not set}:80"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    environment:
      BACKEND_API_PREFIX: ${BACKEND_API_PREFIX?Variable not set}
      BACKEND_HOST: ${BACKEND_HOST?Variable not set}
      BACKEND_PORT: ${BACKEND_PORT?Variable not set}
    networks:
      - app-network
    restart: unless-stopped
    <<: *default-logging

volumes:
  db-data:
  pgadmin-data:
  certificates-data:

networks:
  app-network:
